import 'package:flutter/material.dart';

class AboutSceen extends StatelessWidget {
  const AboutSceen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("AboutSceen"),
      ),
    );
    
  }
}
  
